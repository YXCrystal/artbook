- @comments.each do |comment|
    - user = User.find_by(id: comment.user_id)
    =image_tag user.avatar.url, class: "post_user--avatar"
    %p{:class => "comment_text"} 
        %span{:class => "comment_text--author"} #{user.first_name} #{user.last_name} 
        %span #{comment.body}